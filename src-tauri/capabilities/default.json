{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the DeepSeek Harness main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
